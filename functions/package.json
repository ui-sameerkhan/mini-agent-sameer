{
  "name": "sitepulse-functions",
  "description": "SitePulse push-notification sender and weekly automated backup — first-party Firebase Cloud Functions replacing the dead external Netlify send-push.js / weekly-backup.js, living in the same project as the app's data so they can actually be debugged.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1",
    "@google-cloud/firestore": "^7.10.0"
  },
  "private": true
}
