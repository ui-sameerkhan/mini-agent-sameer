{
  "name": "sitepulse-functions",
  "description": "SitePulse push-notification sender — replaces the external Netlify send-push.js function with a Firebase Cloud Function that lives in the same project as the app's data, so it can actually be debugged.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
